---
import type { FeaturedPost } from '@api/ghost/types';
import PostsShowcaseCarousel, { PostsShowcaseSkeleton } from './PostsShowcaseCarousel';
import {
    filterPostsByLocale,
    type Locale,
    DEFAULT_LOCALE,
    buildLocalePath,
    getUIText,
} from '@lib/i18n';

const MAX_POSTS = 8;

interface Props {
    featuredPosts: FeaturedPost[];
    /**
     * 当前语言
     */
    locale?: Locale;
    /**
     * 是否显示加载状态
     * @default false
     */
    isLoading?: boolean;
    /**
     * 自定义样式类名
     */
    className?: string;
}

const { featuredPosts, locale = DEFAULT_LOCALE, isLoading = false, className = '' } = Astro.props;

// 按语言过滤文章：优先显示当前语言，如果没有则显示其他语言版本
const localizedPosts = filterPostsByLocale(featuredPosts || [], locale);
// 提取文章对象（已按发布日期排序）
const filteredPosts = localizedPosts.map((item) => item.post);
// 限制文章数量为最多8个
const limitedPosts = filteredPosts.slice(0, MAX_POSTS);
// 只把卡片实际需要的字段传给岛屿，避免把完整文章正文(html/excerpt 等)序列化进首页水合载荷
const carouselPosts: FeaturedPost[] = limitedPosts.map((post) => ({
    id: post.id,
    title: post.title,
    url: post.url,
    feature_image: post.feature_image,
    published_at: post.published_at,
    post_type: post.post_type,
    post_category: post.post_category,
    post_series: post.post_series,
    // 可选字段仅在存在时包含（exactOptionalPropertyTypes）
    ...(post.post_type_label !== undefined && { post_type_label: post.post_type_label }),
    ...(post.post_category_label !== undefined && {
        post_category_label: post.post_category_label,
    }),
}));
// 判断是否有更多文章
const hasMorePosts = filteredPosts.length > MAX_POSTS;
// 构建多语言文章列表页 URL
const postsPageUrl = buildLocalePath(locale, 'post-view');
const noPostsText = getUIText('home', 'noPosts', locale);
---

<section class:list={['posts-showcase w-full', className]} aria-label="最近文章">
    {
        isLoading ? (
            <PostsShowcaseSkeleton count={5} client:visible />
        ) : limitedPosts.length > 0 ? (
            <PostsShowcaseCarousel
                posts={carouselPosts}
                postsPageUrl={postsPageUrl}
                showMoreButton={hasMorePosts}
                locale={locale}
                client:visible
            />
        ) : (
            <div class="flex min-h-[12rem] items-center justify-center px-4">
                <p class="rounded-lg border border-[var(--border)] bg-[var(--background)]/70 px-5 py-3 text-sm font-medium text-[var(--muted-foreground)] shadow-sm backdrop-blur-sm">
                    {noPostsText}
                </p>
            </div>
        )
    }
</section>

<style>
    .posts-showcase {
        /* 确保组件不会超出父容器 */
        max-width: 100%;
        overflow: hidden;
    }
</style>

<script is:inline>
    (() => {
        const selector = '[data-home-showcase-scroll]';
        const hydratedAttribute = 'homeShowcaseHydrated';
        const boundContainers =
            window.__solitudeHomeShowcaseBoundContainers ??
            (window.__solitudeHomeShowcaseBoundContainers = new WeakSet());

        const bindHomeShowcaseScroll = () => {
            document.querySelectorAll(selector).forEach((container) => {
                if (!(container instanceof HTMLElement)) return;
                if (boundContainers.has(container)) return;

                boundContainers.add(container);
                const reducedMotionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');

                container.addEventListener(
                    'wheel',
                    (event) => {
                        if (container.dataset[hydratedAttribute] === 'true') return;

                        const dominantDelta =
                            Math.abs(event.deltaX) > Math.abs(event.deltaY)
                                ? event.deltaX
                                : event.deltaY;
                        if (dominantDelta === 0) return;

                        const { scrollLeft, scrollWidth, clientWidth } = container;
                        const canScroll = scrollWidth > clientWidth;
                        const atStart = scrollLeft <= 0 && dominantDelta < 0;
                        const atEnd = scrollLeft >= scrollWidth - clientWidth && dominantDelta > 0;

                        if (!canScroll || atStart || atEnd) return;

                        event.preventDefault();
                        event.stopPropagation();

                        const isHorizontalGesture = Math.abs(event.deltaX) > Math.abs(event.deltaY);
                        const direction = dominantDelta > 0 ? 1 : -1;
                        const card = container.querySelector('.showcase-card');
                        const cardWidth = card?.getBoundingClientRect().width || 304;
                        const scrollDistance = cardWidth + 16;

                        container.scrollBy({
                            left: isHorizontalGesture ? event.deltaX : direction * scrollDistance,
                            behavior:
                                isHorizontalGesture || reducedMotionQuery.matches
                                    ? 'auto'
                                    : 'smooth',
                        });
                    },
                    { passive: false }
                );
            });
        };

        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', bindHomeShowcaseScroll, { once: true });
        } else {
            bindHomeShowcaseScroll();
        }

        if (!window.__solitudeHomeShowcaseReady) {
            window.__solitudeHomeShowcaseReady = true;
            document.addEventListener('astro:page-load', bindHomeShowcaseScroll);
        }
    })();
</script>
